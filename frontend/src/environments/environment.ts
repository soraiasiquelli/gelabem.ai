export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  // Client ID do Google Cloud Console (OAuth "Aplicativo da Web"). Vazio = botão "Continuar com Google" fica escondido.
  googleClientId: '',
  // ID de medição do Google Analytics 4 (público). Vazio = sem rastreamento e sem aviso de cookies.
  gaMeasurementId: ''
};
